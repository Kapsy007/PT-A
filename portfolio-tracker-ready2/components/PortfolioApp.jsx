import React from 'react'; import Card from './ui/Card'; import Button from './ui/Button'; import LinkedAccounts from './LinkedAccounts'; import { useSession, signOut } from 'next-auth/react'; export default function PortfolioApp(){ const {data:session}=useSession(); if(!session) return <div style={{height:'100vh',display:'flex',alignItems:'center',justifyContent:'center'}}>You must sign in <a href='/api/auth/signin'>Sign in</a></div>; return (<div style={{padding:24}}><div style={{display:'flex',justifyContent:'space-between'}}><h1>Portfolio Tracker</h1><div>{session.user?.email} <Button onClick={()=>signOut()}>Logout</Button></div></div><LinkedAccounts onImport={(m)=>console.log('import',m)} /><Card><h3>Equity Holdings</h3><div>No holdings yet.</div></Card></div>)}
